name: xterm
description: Terminal emulator widget for Flutter. Fork of xterm.dart.
version: 4.1.0
homepage: https://github.com/unixshells/xterm-dart

environment:
  sdk: ^3.10.0
  flutter: ">=3.19.0"

dependencies:
  convert: ^3.0.0
  meta: ^1.3.0
  quiver: ^3.0.0
  equatable: ^2.0.3
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^3.0.0

flutter:
